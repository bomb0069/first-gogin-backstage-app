apiVersion: v1
kind: Service
metadata:
  name: first-gogin-app
  namespace: first-gogin-app
  labels:
    app: first-gogin-app
    backstage.io/kubernetes-id: first-gogin-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: first-gogin-app
